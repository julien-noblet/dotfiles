#!/bin/sh

if [ ! -f "/home/{{ .chezmoi.destDir }}/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/{{ .chezmoi.destDir }}/.config/chezmoi"
    chezmoi age decrypt --output "{{ .chezmoi.destDir }}/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/{{ .chezmoi.destDir }}/.config/chezmoi/key.txt"
fi
